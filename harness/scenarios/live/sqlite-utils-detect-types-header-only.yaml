id: AIDD-LIVE-005
scenario_class: live-full-flow
feature_size: small
automation_lane: manual
canonical_runtime: codex
repo:
  url: https://github.com/simonw/sqlite-utils
  default_branch: main
  revision: 8d74ffc93292c604d5827e2b44fffedca0c28c19
objective: Keep sqlite-utils smoke lane deterministic by targeting the header-only
  detect-types crash with bounded patch scope.
task: Run the installed AIDD full-flow live audit against the selected sqlite-utils
  issue and preserve reviewable quality evidence.
stage_scope:
  start: idea
  end: qa
setup:
  commands:
  - uv sync || pip install -e .[dev]
  rationale:
  - Install sqlite-utils development dependencies before the live audit.
  - Confirm the repository environment is ready for targeted regression verification.
aidd_invocation:
  work_item: WI-LIVE-SQLITE-SMOKE
  expected_stage_scope:
    start: idea
    end: qa
feature_source:
  mode: curated-issue-pool
  selection_policy: first-listed
  issues:
  - id: '705'
    title: header-only CSV bugfix
    url: https://github.com/simonw/sqlite-utils/issues/705
    summary: Prevent a crash when `--detect-types` is used on a CSV file that contains
      only a header row and add regression coverage.
    labels:
    - bug
    - csv
operator_execution:
  install_channel: uv-tool
  artifact_source: local-wheel
  execution_cwd: repository-root
  workspace_root: .aidd
  resource_source: packaged-assets
verify:
  commands:
  - uv run pytest -q || pytest -q
  - test -f .aidd/workitems/WI-LIVE-SQLITE-SMOKE/stages/qa/output/stage-result.md
  - test -f .aidd/workitems/WI-LIVE-SQLITE-SMOKE/stages/qa/output/validator-report.md
  pass_conditions:
  - Verification command exits with status 0.
  - Pytest output reports all tests as passed.
  - No new failing tests are introduced relative to baseline.
  - QA stage output artifacts exist for the selected work item.
quality:
  commands:
  - uv run pytest -q || pytest -q
  rubric_profile: live-full
  require_review_status: approved
  allowed_qa_verdicts:
  - ready
  - ready-with-risks
  code_review_required: true
workflow_bundle:
  lane: installed-live-full-flow-audit
  required_artifacts:
  - stage-result.md
  - validator-report.md
  - runtime.log
  - quality-report.md
reference_run:
  run_id: eval-live-005-codex-reference-20260422T123518Z
  recorded_at_utc: '2026-04-22T12:35:29Z'
  runtime: codex
  status: harness_pass
  resolved_revision: 8d74ffc93292c604d5827e2b44fffedca0c28c19
  bundle_root: .aidd/reports/evals/eval-live-005-codex-reference-20260422T123518Z
  execution_model: legacy-source-checkout
  first_failure_signal: none (setup, aidd invocation, and verification all passed).
opencode_reference_run:
  run_id: eval-live-005-opencode-20260422T142733Z
  recorded_at_utc: '2026-04-22T14:28:06Z'
  runtime: opencode
  status: harness_pass
  resolved_revision: 8d74ffc93292c604d5827e2b44fffedca0c28c19
  bundle_root: .aidd/reports/evals/eval-live-005-opencode-20260422T142733Z
  execution_model: legacy-source-checkout
  first_failure_signal: none (setup, aidd invocation, and verification all passed).
limits:
  patch_budget_files: 8
  timeout_minutes: 90
interview:
  required: false
runtime_targets:
- codex
- opencode
- claude-code
