id: AIDD-LIVE-005
repo:
  url: https://github.com/simonw/sqlite-utils
  default_branch: main
  revision: 8d74ffc93292c604d5827e2b44fffedca0c28c19
issue:
  id: 705
  title: header-only CSV bugfix
objective: Keep sqlite-utils smoke lane deterministic by targeting the header-only
  detect-types crash with bounded patch scope.
task: Prevent a crash when --detect-types is used on a CSV file that contains only
  a header row. Add regression coverage.
stage_scope:
  start: plan
  end: qa
setup:
  commands:
  - uv sync || pip install -e .[dev]
  rationale:
  - Install sqlite-utils development dependencies before the smoke run.
  - Confirm the repository environment is ready for targeted regression verification.
aidd_invocation:
  command:
  - uv
  - run
  - aidd
  - run
  work_item: WI-LIVE-SQLITE-SMOKE
  work_item_flag: --work-item
  runtime_flag: --runtime
  expected_stage_scope:
    start: plan
    end: qa
verify:
  commands:
  - uv run pytest -q || pytest -q
  pass_conditions:
  - Verification command exits with status 0.
  - Pytest output reports all tests as passed.
  - No new failing tests are introduced relative to baseline.
reference_run:
  run_id: eval-live-005-reference-20260422T090823Z
  recorded_at_utc: '2026-04-22T09:08:35Z'
  runtime: generic-cli
  status: harness_pass
  resolved_revision: 8d74ffc93292c604d5827e2b44fffedca0c28c19
  bundle_root: .aidd/reports/evals/eval-live-005-reference-20260422T090823Z
  first_failure_signal: none (setup, aidd invocation, and verification all passed).
limits:
  patch_budget_files: 8
  timeout_minutes: 20
interview:
  required: false
runtime_targets:
- claude-code
- generic-cli
