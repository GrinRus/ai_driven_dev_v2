# Implement Example Bundle

This bundle contains two worked `implement` variants:

- `success/` - implementation output with evidence-backed edits and validator pass.
- `repair-needed/` - implementation output that fails validation and requires repair.
