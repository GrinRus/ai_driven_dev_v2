# Plan Example Bundle

This bundle contains two worked `plan` variants:

- `valid/` - a reviewable plan with validator pass.
- `invalid/` - a plan that fails validation due to sequencing and readiness gaps.
