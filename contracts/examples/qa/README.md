# QA Example Bundle

This bundle contains two worked `qa` variants:

- `success/` - QA output with evidence-backed verdict and validator pass.
- `repair-needed/` - QA output that fails validation and requires repair.
