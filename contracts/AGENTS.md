# AGENTS.md

This directory defines durable human-readable workflow contracts.

## Rules

- Keep contracts Markdown-first.
- Put reusable document shapes under `documents/`.
- Put stage-specific requirements under `stages/`.
- Change docs before validators or orchestration code when behavior changes.
