# Tasklist

## Task summary

Eleven malformed cards must remain located and fail closed.

## Ordered tasks

### TL-1 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-1-AC1: Missing outcome is reported at the card heading.

### TL-2 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-2-AC1: Missing outcome is reported at the card heading.

### TL-3 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-3-AC1: Missing outcome is reported at the card heading.

### TL-4 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-4-AC1: Missing outcome is reported at the card heading.

### TL-5 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-5-AC1: Missing outcome is reported at the card heading.

### TL-6 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-6-AC1: Missing outcome is reported at the card heading.

### TL-7 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-7-AC1: Missing outcome is reported at the card heading.

### TL-8 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-8-AC1: Missing outcome is reported at the card heading.

### TL-9 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-9-AC1: Missing outcome is reported at the card heading.

### TL-10 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-10-AC1: Missing outcome is reported at the card heading.

### TL-11 — Missing outcome

- Dominant deliverable: `src/example.py` validates the field.
- In scope: `src/example.py` and `tests/test_validator.py`.
- Acceptance criteria:
  - TL-11-AC1: Missing outcome is reported at the card heading.

## Dependencies

- TL-1: none
- TL-2: TL-1
- TL-3: TL-2
- TL-4: TL-3
- TL-5: TL-4
- TL-6: TL-5
- TL-7: TL-6
- TL-8: TL-7
- TL-9: TL-8
- TL-10: TL-9
- TL-11: TL-10

## Verification notes

- TL-1: `pytest tests/test_validator.py -q`
- TL-2: `pytest tests/test_validator.py -q`
- TL-3: `pytest tests/test_validator.py -q`
- TL-4: `pytest tests/test_validator.py -q`
- TL-5: `pytest tests/test_validator.py -q`
- TL-6: `pytest tests/test_validator.py -q`
- TL-7: `pytest tests/test_validator.py -q`
- TL-8: `pytest tests/test_validator.py -q`
- TL-9: `pytest tests/test_validator.py -q`
- TL-10: `pytest tests/test_validator.py -q`
- TL-11: `pytest tests/test_validator.py -q`
