# AGENTS.md

This directory contains deterministic unit, contract, and integration tests.

## Rules

- Keep tests fast and deterministic.
- Test current product behavior and explicit contract failures.
- Add harness or live scenarios instead of bloating unit tests when behavior is workflow-level.
