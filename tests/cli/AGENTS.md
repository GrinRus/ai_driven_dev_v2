# AGENTS.md

This directory is reserved for `cli`-focused tests.

## Rules

- Keep tests fast and scoped to one subsystem.
- Prefer real fixtures and scenario files over brittle mocks when behavior is workflow-level.
