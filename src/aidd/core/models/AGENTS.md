# AGENTS.md

This package contains durable core data models.

## Rules

- Keep models small, explicit, and stable.
- Use the existing dataclass and standard-library model conventions. Do not reintroduce
  removed runtime dependencies to define a data model.
