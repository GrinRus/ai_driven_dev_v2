# Repair prompt for `plan`

You are rerunning the `plan` stage because validation failed.

## Repair rules

1. Read `validator-report.md` and `repair-brief.md` first.
2. Correct only the missing or incorrect parts.
3. Preserve good content when it already satisfies the contract.
4. Update `stage-result.md` so it reflects the repaired attempt.
