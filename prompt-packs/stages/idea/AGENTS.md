# AGENTS.md

This directory contains the `idea` stage prompt pack.

## Rules

- Keep the prompt aligned with `contracts/stages/idea.md`.
- Tell the runtime which Markdown files it must read and write.
- Keep repair guidance specific to validator findings.
- Use `interview.md` for question-asking behavior, not for normal stage execution.
